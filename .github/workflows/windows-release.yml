name: Windows release 1.0.1

on:
  workflow_dispatch:
  push:
    branches: [main]
    tags: ['v*']

jobs:
  test-and-build:
    runs-on: windows-latest
    steps:
      - name: Отримати код
        uses: actions/checkout@v4
      - name: Встановити Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Встановити інструменти
        shell: pwsh
        run: python -m pip install --upgrade pip pytest pyinstaller pillow
      - name: Синтаксична перевірка
        shell: pwsh
        run: python -m compileall -q core desktop_final.py desktop_release.py tests
      - name: Підготувати ресурси
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path build_assets\assets | Out-Null
          if (!(Test-Path "language-russian.jpg")) { throw "language-russian.jpg не знайдено" }
          if (!(Test-Path "sounds\opiat-rabota.mp3")) { throw "sounds/opiat-rabota.mp3 не знайдено" }
          python -c "from PIL import Image; Image.open('language-russian.jpg').convert('RGB').save('build_assets/assets/moskal.png', 'PNG')"
      - name: Автоматичні тести
        shell: pwsh
        run: pytest -q
      - name: Зібрати EXE
        shell: pwsh
        run: pyinstaller --noconfirm --clean --onefile --windowed --name "Dashboard_1.0.1" --add-data "sounds;sounds" --add-data "build_assets/assets/moskal.png;assets" desktop_release.py
      - name: Перевірити EXE
        shell: pwsh
        run: |
          if (!(Test-Path "dist\Dashboard_1.0.1.exe")) { throw "EXE не створено" }
          $size=(Get-Item "dist\Dashboard_1.0.1.exe").Length
          if ($size -lt 100000) { throw "EXE має підозріло малий розмір" }
          Get-Item "dist\Dashboard_1.0.1.exe" | Format-List Name,Length,FullName
      - name: Встановити Inno Setup
        shell: pwsh
        run: choco install innosetup -y --no-progress
      - name: Зібрати Setup
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path installer\output | Out-Null
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer\Dashboard.iss
          if (!(Test-Path "installer\output\Дашборд_розпоряджень_1.0.1_Setup.exe")) { throw "Setup не створено" }
      - name: Зберегти EXE
        uses: actions/upload-artifact@v4
        with:
          name: Dashbord-Windows-1.0.1
          path: dist/Dashboard_1.0.1.exe
          if-no-files-found: error
      - name: Зберегти Setup
        uses: actions/upload-artifact@v4
        with:
          name: Dashbord-Setup-1.0.1
          path: installer/output/Дашборд_розпоряджень_1.0.1_Setup.exe
          if-no-files-found: error
